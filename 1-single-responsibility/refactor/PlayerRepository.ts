// Clase dedicada a la persistencia de datos

import { Player } from './Player';

class PlayerRepository {

  public savePlayer(player: Player) {
    // guardar jugador
  }

  public deletePlayer(player: Player) {
    // eliminar jugador
  }
}

export { PlayerRepository };
